export enum envEnum {
    development = 'development',
    staging = 'staging',
    production = 'production'
}

export enum userTypeEnum {
    vendor = 'vendor',
    user = 'user',
    admin = 'admin'
}

export enum categoryTypesEnum {
    product = 'product',
    service = 'service'
}
